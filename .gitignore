# Solana local validator logs (generated by anchor test / solana-test-validator)
test-ledger/

# Internal tooling directories (not project code)
.sixth/
ares.toml
ares.log
ares-policy.toml
ares-output/

# TypeScript incremental build cache
*.tsbuildinfo

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Rust / Cargo build artifacts
target/
**/target/

# Node.js dependencies & build caches
node_modules/
**/node_modules/

# Next.js build output
.next/
**/.next/

# Vite / Vitest caches
.vite/
**/.vite/

# Local environment files (never commit secrets — only .env.example is tracked)
.env
.env.local
.env.*.local
**/.env
**/.env.local
**/.env.*.local
.vercel
