/**
 * Twitter/X-specific card image.
 * Re-exports the same OG image so both /opengraph-image and /twitter-image
 * serve the BlockBite branded preview card.
 */
export { default, alt, size, contentType, runtime } from './opengraph-image';
