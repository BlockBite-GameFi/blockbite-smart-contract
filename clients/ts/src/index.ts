/**
 * @blockbite/clients — TypeScript client library for BlockBite Anchor program.
 *
 * Exports:
 *   - Vesting client (stream creation, withdrawal, cancellation, milestone, close)
 *   - Campaign client (campaign/milestone creation, proof submission, game verification, claiming)
 *   - PDA derivation helpers
 *   - Account decoding utilities
 */

export * from './vesting-client';
export * from './campaign-client';
